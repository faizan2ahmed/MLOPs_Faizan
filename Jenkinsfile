pipeline {
    agent any

    stages {
        stage('Cloning repository') {
            steps {
                git 'https://github.com/faizan2ahmed/MLOPs_Faizan.git'
            }
        }
    }
}
